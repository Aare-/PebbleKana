#include <pebble.h>
#include "quiz.h"
#include "common.h"

static struct QuizUi {
	Window* window;
} ui;
  
static void load(Window* window) {
  
}

static void unload(Window* window) {
  
}

INIT(quiz)